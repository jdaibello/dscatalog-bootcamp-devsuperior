import React from "react";
import { Text, View } from "react-native";

const Users = () => {
  return (
    <View>
      <Text>Tela de Usuários (tela temporária)</Text>
    </View>
  );
};

export default Users;
